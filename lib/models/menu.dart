import'package:flutter/material.dart';

class Menu {
  int type;
  String title;
  String image;
  Color color;

  Menu({
    required this.type,
    required this.title,
    required this.image,
    required this.color,
  });
}